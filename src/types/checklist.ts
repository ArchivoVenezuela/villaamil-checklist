// Re-export from unified types for backwards compatibility
export type {
  Task,
  Section,
  Checklist,
  ChecklistProgress,
  ChecklistTemplate,
  ChecklistSection,
  ChecklistTask,
  ChecklistEntry,
  AppSettings,
  AppBackup,
  EntryStatus,
} from './index'
